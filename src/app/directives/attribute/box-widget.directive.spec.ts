import { BoxWidgetDirective } from './box-widget.directive';

describe('BoxWidgetDirective', () => {
  it('should create an instance', () => {
    const directive = new BoxWidgetDirective();
    expect(directive).toBeTruthy();
  });
});
